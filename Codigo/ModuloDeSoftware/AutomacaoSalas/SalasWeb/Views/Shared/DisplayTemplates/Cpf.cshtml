@model string

@{
    if (!string.IsNullOrWhiteSpace(Model) && Model.Length == 11)
    {
        var cpfFormatado = $"{Model.Substring(0, 3)}.{Model.Substring(3, 3)}.{Model.Substring(6, 3)}-{Model.Substring(9, 2)}";
        @cpfFormatado
    }
    else
    {
        @Model
    }
}